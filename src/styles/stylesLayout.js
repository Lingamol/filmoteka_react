const colors = {
  primaryOrangeColor: '#ff6b08',
  secondaryOrangeColor: '#ff6b01',
  whiteColor: '#ffffff',
  blackColor: '#000000',
  redlLiningColor: '#ff001b',
  footerTextColor: '#545454',
};

const timingFunction = { cb1: 'cubic-bezier(0.4, 0, 0.2, 1)' };

const duration = { dur250: '250ms' };

export { colors, timingFunction, duration };
